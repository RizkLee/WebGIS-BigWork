# 快速开发指南

## 🎯 如何添加新内容

### 添加新的板块

打开 `src/data/sections.ts`,在 `sectionData` 数组末尾添加:

```typescript
{
  title: '你的新板块标题',
  items: [
    // 在这里添加条目
  ]
}
```

### 添加新的条目

在对应板块的 `items` 数组中添加:

```typescript
{
  title: '条目标题',
  links: [
    // 普通链接
    { type: 'link', text: '按钮文字', url: 'https://example.com' },
    // 二维码链接
    { type: 'qr', text: '官微', qrImage: '/Pictures/your-qr-code.jpg' }
  ],
  description: '这里是描述,支持<a href="#">HTML标签</a>'
}
```

### 添加新的图片资源

1. 将图片文件放到 `public/Pictures/` 目录
2. 在数据配置中使用 `/Pictures/文件名.jpg` 引用

## 🎨 如何修改样式

### 修改主题色

编辑 `src/style.css`,找到以下颜色值:
- `#8F0100` - 深红色(标题、边框)
- `#b24646` - 浅红色(链接)
- `#F5F5F5` - 背景灰色

建议使用CSS变量统一管理:

```css
:root {
  --primary-color: #8F0100;
  --link-color: #b24646;
  --bg-color: #F5F5F5;
}
```

### 修改组件样式

每个组件都有自己的 `<style scoped>` 部分,修改即可:
- `SiteHeader.vue` - 头部样式
- `ContentSection.vue` - 板块样式
- `SectionItem.vue` - 条目样式
- `QRButton.vue` - 按钮样式

## 🔧 组件说明

### SiteHeader (网站头部)
```vue
<SiteHeader />
```
无需传参,显示Logo和欢迎文字

### ContentSection (内容板块)
```vue
<ContentSection title="板块标题">
  <!-- 在这里放SectionItem -->
</ContentSection>
```

### SectionItem (可展开条目)
```vue
<SectionItem
  title="条目标题"
  :links="[
    { type: 'link', text: '按钮', url: 'https://...' }
  ]"
>
  <div>描述内容</div>
</SectionItem>
```

### QRButton (二维码按钮)
```vue
<QRButton
  text="按钮文字"
  qr-image="/Pictures/qr.jpg"
/>
```

## 📝 常见需求示例

### 示例1: 添加一个新的学习资源板块

```typescript
// src/data/sections.ts
{
  title: '学习资源',
  items: [
    {
      title: 'Coursera',
      links: [
        { type: 'link', text: '访问', url: 'https://www.coursera.org/' }
      ],
      description: '全球知名的在线学习平台,提供各类课程。'
    },
    {
      title: '中国大学MOOC',
      links: [
        { type: 'link', text: '官网', url: 'https://www.icourse163.org/' },
        { type: 'qr', text: 'APP下载', qrImage: '/Pictures/mooc-qr.jpg' }
      ],
      description: '国内优质的慕课平台。'
    }
  ]
}
```

### 示例2: 修改背景图片

1. 将新图片放到 `public/Pictures/`
2. 编辑 `src/style.css`:

```css
body::before {
  background-image: url('/Pictures/your-new-background.jpg');
}
```

### 示例3: 添加深色模式

1. 在 `src/style.css` 添加:

```css
@media (prefers-color-scheme: dark) {
  body {
    background-color: #1a1a1a;
    color: #ffffff;
  }
  
  .section {
    background-color: #2a2a2a;
  }
}
```

## 🚀 性能优化建议

1. **图片优化**: 使用WebP格式,压缩图片大小
2. **懒加载**: 对大量图片使用懒加载
3. **代码分割**: 如果板块很多,考虑按需加载

## 🐛 调试技巧

### 查看数据结构
在浏览器控制台:
```javascript
// 查看所有板块数据
console.log(window.__sections__)
```

### 热重载问题
如果修改后没有更新:
1. 刷新浏览器
2. 重启开发服务器: `Ctrl+C` 然后 `npm run dev`

## 📦 部署

### 构建生产版本
```bash
npm run build
```
构建结果在 `dist/` 目录

### 部署到静态服务器
将 `dist/` 目录内容上传到:
- GitHub Pages
- Vercel
- Netlify
- 学校服务器

## ✨ 祝你开发愉快!

有问题可以查看:
- `MIGRATION.md` - 详细的项目结构说明
- `迁移完成总结.md` - 完整功能列表
